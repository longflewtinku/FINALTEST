<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.view.View"/>
        <import type="androidx.core.content.ContextCompat" />
        <variable
            name="viewModel"
            type="com.linkly.launcher.viewmodels.UnattendedIdleScreenViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginVertical="5dp"
        android:layout_marginHorizontal="2dp"
        android:background="@android:color/transparent"
        android:id="@+id/rootViewrootView"
        tools:context="com.linkly.launcher.LockedDownActivity">

        <ImageView
            android:id="@+id/header"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_gravity="center_horizontal"
            bind:imageBitmap="@{viewModel.brandDisplayLogoIdle}"
            android:scaleType="centerInside"
            app:layout_constraintVertical_weight="0.7"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/kiosk_screen_text"/>

        <TextView
            android:id="@+id/kiosk_screen_text"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="15dp"
            android:layout_marginHorizontal="18dp"
            android:gravity="center"
            android:textStyle="bold"
            android:lineSpacingExtra="2sp"
            app:autoSizeTextType="uniform"
            app:autoSizeMinTextSize="16sp"
            app:autoSizeMaxTextSize="50sp"
            android:textColor="@android:color/black"
            android:text="@{viewModel.messageText}"
            android:visibility="@{viewModel.messageText == null? View.GONE : View.VISIBLE}"
            app:layout_constraintVertical_weight="0.3"
            app:layout_constraintTop_toBottomOf="@+id/header"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
